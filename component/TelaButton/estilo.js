import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
    buttonText: {
        fontSize: 18,
        fontWeight: '600',
        color: '#D0D0D0'
    }
})
export default estilo;