import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
    buttonText: {
        fontSize: 18,
        fontWeight: '600',
        color: '#F0F0F0'
    }
})
export default estilo;